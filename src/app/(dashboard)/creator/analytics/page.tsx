// Thin wrapper — underlying Analytics page (creator-focused: queries
// wallet_transactions_archive + campaigns by creator_id) lives at
// /dashboard/analytics. Brand analytics is a separate, future page.
export { default } from "../../dashboard/analytics/page";
