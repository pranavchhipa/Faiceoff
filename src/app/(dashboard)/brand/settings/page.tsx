// Thin wrapper — underlying Settings page is role-aware (see
// /creator/settings for the same pattern). Mounted at /brand/settings so
// brand sidebar has a stable role-prefixed entry point.
export { default } from "../../dashboard/settings/page";
