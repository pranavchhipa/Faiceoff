import type { ReactNode } from "react";

// AuthShell provides full-page layout — this layout is intentionally minimal.
export default function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
